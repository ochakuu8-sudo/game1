{
  "name": "game1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "serve": "vite preview --host 0.0.0.0",
    "check": "node --check src/main.js && node --check src/medalEconomy.js",
    "test": "node tests/medal-economy.test.mjs"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
